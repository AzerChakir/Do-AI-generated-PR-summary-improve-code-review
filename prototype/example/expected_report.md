# PR Decomposition — Mixed PR: avatars, orders speedup, refactor

**Change size:** 8 file(s), 66 additions, 15 deletions
**Verdict:** multiple PRs

## Overall summary

The change bundles an avatar-upload feature, a query performance fix, and a utility refactor with a dependency bump, all in one pull request.

## Concerns detected (3)

### 1. add user avatar upload

- **Type:** modifies behavior
- **Files:** users/uploads.py, users/views.py, users/templates/avatar.html
- **Why grouped:** These files implement the storage and display of avatars.

### 2. fix slow orders query

- **Type:** modifies behavior
- **Files:** orders/queries.py, orders/migrations/0042_order_lineitem_index.py
- **Why grouped:** The query and its migration were changed to add an index and reduce N+1 fetches.

### 3. refactor utils and bump storage dep

- **Type:** modifies behavior
- **Files:** core/utils.py, users/views.py, requirements.txt, pyproject.toml
- **Why grouped:** Helper code moved out of views and the storage dependency version changed, touching unrelated flows.
- **MIXED:** users/views.py mixes avatar upload with the utils refactor, and the dependency bumps ride along.

## Mixed-concern highlights

- **refactor utils and bump storage dep** — users/views.py mixes avatar upload with the utils refactor, and the dependency bumps ride along.

## Flags
- [warning] **mixed-concern** — concern 'refactor utils and bump storage dep' bundles unrelated topics: users/views.py mixes avatar upload with the utils refactor, and the dependency bumps ride along. (core/utils.py, users/views.py, requirements.txt, pyproject.toml)
- [info] **structure** — change covers 3 distinct concerns; a split into multiple PRs looks beneficial

## Suggested decomposition

- **Suggested PR 1: Add user avatar upload**
  - Files: users/uploads.py, users/views.py, users/templates/avatar.html
- **Suggested PR 2: Fix slow orders query**
  - Files: orders/queries.py, orders/migrations/0042_order_lineitem_index.py
- **Suggested PR 3: Refactor utils and bump storage dep**  — **mixed concern, split internally**
  - Files: core/utils.py, users/views.py, requirements.txt, pyproject.toml

### Recommended merge order

Merge the self-contained change first (`add user avatar upload`), then the dependent ones in order: fix slow orders query, refactor utils and bump storage dep. Keep refactors and dependency bumps last so a regression is easy to bisect.

## Notes for the author

- Split `refactor utils and bump storage dep`: its files mix unrelated topics (users/views.py mixes avatar upload with the utils refactor, and the dependency bumps ride along.).

---
_Generated by PR Decomposer (research prototype). Approve, edit, or discard freely._

---

## AI change summary (old → new)

### add user avatar upload

- **Before:** User profiles rendered a plain text link; uploads were handled on the client with no storage abstraction, so files were not persisted server-side.
- **After:** A dedicated users/uploads module persists the file, the view returns the stored storage key, and the avatar template renders the uploaded image.
- **Impact:** Users get persistent avatars; the storage dependency now affects runtime behaviour and must be configured in the deployment.

### fix slow orders query

- **Before:** orders/queries.py ran N+1 queries on the line items and relied on the default table without a covering index.
- **After:** The listing query is rewritten to prefetch line items, and migration 0042 adds an index on the line-item table.
- **Impact:** Order listing is faster on large accounts; requires the new migration to be applied before rollout.

### refactor utils and bump storage dep

- **Before:** Utility helpers lived in users/views.py and the storage package was pinned to an old version.
- **After:** Helpers moved to core/utils.py and the storage dependency is upgraded in requirements.txt and pyproject.toml.
- **Impact:** No behaviour change expected, but the dependency bump touches unrelated flows and should be verified independently.

## Review of the new code

**[important] awaiting storage key breaks error path** (users/views.py, users/uploads.py)
The new upload view awaits the storage write in the success path but shares the legacy error template, so users get a stale message if storage fails. Handle the failure branch explicitly.

**[minor] mixed-concern file not split** (users/views.py)
users/views.py still mixes the avatar feature with the utils refactor; the dependency bump in the same PR makes bisecting a regression harder than needed.

**Verdict:** The new code is functional and the orders query fix is solid, but the mixed users/views.py and the storage error path should be addressed before merge; splitting the PR would make the review much easier.
